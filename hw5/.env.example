# ==========================================
# Echo 專案環境變數設定
# ==========================================
# 
# 使用方式：
# 1. 複製此檔案為 .env
# 2. 填入實際的值
# 3. 不要將 .env 提交到 Git
#
# ==========================================

# ------------------------------------------
# 資料庫設定
# ------------------------------------------

# 開發環境使用 SQLite（簡單快速）
DATABASE_URL="file:./dev.db"

# 生產環境使用 PostgreSQL（取消註解並填入實際值）
# DATABASE_URL="postgresql://username:password@localhost:5432/echo"
# 或 Vercel Postgres
# DATABASE_URL="your_vercel_postgres_url"


# ------------------------------------------
# NextAuth 設定
# ------------------------------------------

# 應用程式網址
# 開發環境
NEXTAUTH_URL="http://localhost:3000"
# 生產環境（部署時修改）
# NEXTAUTH_URL="https://your-app.vercel.app"

# NextAuth Secret（必須）
# 產生方式：openssl rand -base64 32
NEXTAUTH_SECRET="請執行 openssl rand -base64 32 產生並取代此處"


# ------------------------------------------
# Google OAuth 設定
# ------------------------------------------
# 取得方式：https://console.cloud.google.com/
# 重定向 URI：http://localhost:3000/api/auth/callback/google

GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"


# ------------------------------------------
# GitHub OAuth 設定（可選）
# ------------------------------------------
# 取得方式：https://github.com/settings/developers
# 回調 URL：http://localhost:3000/api/auth/callback/github

GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"


# ------------------------------------------
# Facebook OAuth 設定（可選）
# ------------------------------------------
# 取得方式：https://developers.facebook.com/
# OAuth 重定向 URI：http://localhost:3000/api/auth/callback/facebook

FACEBOOK_CLIENT_ID="your-facebook-app-id"
FACEBOOK_CLIENT_SECRET="your-facebook-app-secret"


# ------------------------------------------
# Pusher 設定（選用，用於即時更新）
# ------------------------------------------
# 取得方式：https://pusher.com/
# 免費方案：200k 訊息/天，100 並發連接

# 公開金鑰（前端使用）
NEXT_PUBLIC_PUSHER_KEY="your-pusher-key"

# 私密金鑰（後端使用）
PUSHER_SECRET="your-pusher-secret"

# App ID
PUSHER_APP_ID="your-pusher-app-id"

# Cluster（例如：ap3, us2, eu）
NEXT_PUBLIC_PUSHER_CLUSTER="your-pusher-cluster"


# ------------------------------------------
# 其他設定（選用）
# ------------------------------------------

# Cloudinary（圖片上傳，如果需要）
# NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=""
# CLOUDINARY_API_KEY=""
# CLOUDINARY_API_SECRET=""

# 郵件服務（通知功能，如果需要）
# EMAIL_SERVER="smtp://username:password@smtp.example.com:587"
# EMAIL_FROM="noreply@echo.app"


# ==========================================
# 注意事項
# ==========================================
#
# 1. 至少需要設定一個 OAuth provider（建議 Google）
# 2. NEXTAUTH_SECRET 必須設定
# 3. 修改 .env 後需要重新啟動開發伺服器
# 4. 生產環境請使用 PostgreSQL
# 5. 不要將 .env 檔案提交到 Git！
#
# ==========================================
